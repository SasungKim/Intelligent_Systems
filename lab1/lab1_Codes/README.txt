runlab1.m: the main file - It will operates the decision algorithm and result mean, std, probabilities we got and the result (class)
lab1.m: question 6. - It will use Sepal Length (x1) of training and test set
lab1_x2.m: question 1,2 - It will use Sepal Width (x2) of training and test set